services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=duckytime'
      - 'MYSQL_PASSWORD=secret'
      - 'MYSQL_ROOT_PASSWORD=verysecret'
      - 'MYSQL_USER=duckytime'
    ports:
      - '3306'
